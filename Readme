To begin, create a new project in Google AppScript. Next, write your script code in a .gs file and create another file for your form code. Once you've finished, run the script. From the "Deploy" dropdown menu, select "Test Deployment" to execute the code in any browser. Simply copy the generated link and open it in your preferred web browser.

